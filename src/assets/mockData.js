export const mockData = Array.from({ length: 150 }, (_, i) => ({
    id: i,
    fechaAsignacion: `2025-11-${String((i % 30) + 1).padStart(2, '0')}`,
    incidencia: `INC-${45000 + i}`,
    cliente: i % 3 === 0 ? 'SHELL' : 'G500',
    estadoGuia: i % 2 === 0 ? 'CERRADA' : 'EN TRANSITO',
    fechaEnvio: '2025-11-20',
    fechaLlegada: '2025-11-25',
    merchantId: `MERCH-${8800 + i}`,
    guiaDhl: `DHL-${999999 + i}`,
    direccion: `Av. Reforma ${i}, Centro`,
    colonia: 'Centro Historico',
    ciudad: i % 4 === 0 ? 'CDMX' : (i % 4 === 1 ? 'GUADALAJARA' : 'MONTERREY'),
    estadoMx: i % 4 === 0 ? 'CDMX' : (i % 4 === 1 ? 'JALISCO' : 'NUEVO LEON'),
    tipoServicio: i % 2 === 0 ? 'Correctivo' : 'Preventivo',
    descripcion: 'Falla en terminal bancaria',
    equipoReportado: 'VX520',
    equipoEnviar: 'VX520-V3',
    nombreTecnico: i % 3 === 0 ? 'Juan Perez' : 'Maria Lopez',
    estatusCierre: i % 5 === 0 ? 'ABIERTA' : 'CERRADA',
    transporteEstimado: 100 + (i % 5) * 20,
}));